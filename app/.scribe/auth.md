# Authenticating requests

To authenticate requests, include a **`Authorization`** header with the value **`"{YOUR_SESSION}"`**.

All authenticated endpoints are marked with a `requires authentication` badge in the documentation below.

This API uses session-based authentication. You need to log in first to access most endpoints.
